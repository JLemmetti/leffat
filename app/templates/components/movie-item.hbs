
<div class="info">
    <h2><a href="{{movie.link}}" target="_blank">{{movie.title}}</a></h2>
    <p>Nähty: {{moment-format movie.watchDate 'D.M.YYYY'}}</p>
    <pre>{{movie.notes}}</pre>
</div>

{{#if session.isAuthenticated}}
<div class="actions">
    <button {{action 'editMovie' movie}}>Muokkaa</button>
    <button {{action 'deleteMovie' movie}} class="delete">Poista</button>
</div>
{{/if}}
